<!DOCTYPE html>
<html lang="no">

<head>
    <meta charset="UTF-8"/>
    <style>
        * {
            font-family: "Source Sans Pro", SourceSansPro, Source_Sans_Pro, ArialSystem, sans-serif;
        }

        @page {
            @bottom-right {
                font-family: "Source Sans Pro", SourceSansPro, Source_Sans_Pro, ArialSystem, sans-serif;
                font-size: 12px;
                content: "Side "counter(page) " av "counter(pages);
            }
        }

        .header {
            margin-bottom: 40px;
        }

        .navLogo {
            height: 75px;
            margin-bottom: 80px;
        }

        .personalia {
            clear: both;
            padding-bottom: 16px;
        }

        .personalia p {
            margin: 0;
        }

        .dato {
            text-align: left;
        }

        .mainContent {
            margin-bottom: 40px;
        }

        .pagebreak {
            page-break-before: always;
        }
        .info {
            margin-bottom: 1rem;
        }

        .info p {
            margin-top: 0;
        }

        h3 {
            margin-bottom: 4px;
        }

        .hilsen {
            margin-top: 64px;
        }

        td.left {
            text-align: left;
        }

        td.center {
            text-align: center;
        }

        td.right {
            text-align: right;
        }

        .hilsen p {
            padding: 0;
            margin: 0;
        }

        .keyValueOppsummering__heading {
            margin-bottom: 0;
        }

        .keyValueOppsummering {
            border-collapse: collapse;
            width: 100%;
            clear: both;
        }
        .keyValueOppsummering__title {
            margin-bottom: 0;
        }
        .keyValueOppsummering__value {
            text-align: right;
        }

        .keyValueOppsummering__keyInfo {
            list-style: none;
            margin: 0;
            padding-left: 16px;
            font-size: 0.8em;
        }

        .fritekst {
            white-space: pre-wrap;
            margin-bottom: 1rem;
            word-wrap: break-word;
        }

        .preline{
            white-space: pre-line;
        }

        .tabell {
            width: 100%;
            border-collapse: collapse;
            table-layout: fixed;
        }
        .tabell-auto {
            width: 100%;
            border-collapse: collapse;
            table-layout: auto;
        }
        .tabellcol {
            border: 1px solid black;
            padding-left: 0.2em;
            padding-right: 0.2em;
        }

        .right-align {
            text-align: right;
        }

    </style>
    <title>Tiltakspenger</title>
</head>

<body>
    <div class="main">
        {{#with personalia}}
            <div class="header">
                <img class="navLogo" src="{{image "Navlogo.png"}}" />

                <div class="personalia">
                    <p>Navn: {{fornavn}} {{etternavn}}</p>
                    <p>Fødselsnummer: {{ident}}</p>
                    <p>Saksnummer: {{saksnummer}}</p>
                    <p class="right-align">Dato: {{iso_to_long_date datoForUtsending}}</p>
                </div>
            </div>
        {{/with}}
        {{> @partial-block}}
        {{> tpts/partials/hilsen }}
    </div>
</body>

</html>
